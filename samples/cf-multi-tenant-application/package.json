{
  "scripts": {
    "format": "prettier --write . ",
    "clean-install": "rm -rf node_modules package-lock.json && npm i"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.7.1"
  },
  "engines": {
    "node": "^16.0.0",
    "npm": "^8.0.0"
  }
}
